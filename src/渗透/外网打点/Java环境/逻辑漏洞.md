---
data: 2024-07-24
关联:
  - "[[book/知识笔记/开发/Java开发/Java开发]]"
---
任意用户登录指的是，在登陆系统时。登陆的是哪个用户可以由用户自己指定，可能是因为没有校验userid和username/password的关系导致的。
我们先直接看一下代码
```
@LoginRequire
@RequestMapping(value = "/login", method = RequestMethod.POST)
public String login(@RequestParam(name = "loginid") String loginid,@RequestParam(name = "username") String loginid,@RequestParam(name = "password") String password) {
  ...
  String userid = db.excute("select userid from user_table where username = " + username + " and password = " + password); //不是注入问题
 	if(userid != null){
		return loginid;
  }else{
    return false;
  }
}
```